name: Build
description: 'Setup enviroment and build code'
runs:
   using: composite
   steps:
      - name: Setup
        uses: ./.github/actions/setup

      - name: Build project
        run: yarn add
             yarn build
        shell: bash
